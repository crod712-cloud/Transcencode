# Transcencode CLI-first stabilization

The immediate deliverable is the standalone Windows GUI that drives HandBrakeCLI without opening an external console window. Native HandBrake WPF integration remains on its existing branch and is not the release path until the CLI application is stable.
