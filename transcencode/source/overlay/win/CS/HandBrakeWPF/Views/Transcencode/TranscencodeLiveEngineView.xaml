<UserControl x:Class="HandBrakeWPF.Views.TranscencodeLiveEngineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="720" MinHeight="430">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="#6E7781" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Live Engine" FontSize="18" FontWeight="SemiBold"/>
                    <TextBlock Text="Actual HandBrake engine log with progress, ETA, finish time, and average speed." Margin="0,4,0,0"/>
                </StackPanel>
                <Button Grid.Column="1" Content="Refresh" Command="{Binding RefreshCommand}" Padding="12,6" Margin="8,0,0,0"/>
                <Button Grid.Column="2" Content="Clear View" Command="{Binding ClearCommand}" Padding="12,6" Margin="8,0,0,0"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                <TextBlock Text="Status" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding Status}" TextWrapping="Wrap"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="0,0,8,0">
                <TextBlock Text="Progress" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding ProgressText}"/>
            </StackPanel>
            <StackPanel Grid.Column="2" Margin="0,0,8,0">
                <TextBlock Text="ETA remaining" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding Eta}"/>
            </StackPanel>
            <StackPanel Grid.Column="3" Margin="0,0,8,0">
                <TextBlock Text="Estimated finish" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding EstimatedFinish}"/>
            </StackPanel>
            <StackPanel Grid.Column="4">
                <TextBlock Text="Average speed" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding Speed}"/>
            </StackPanel>
        </Grid>

        <TextBox x:Name="LogBox" Grid.Row="2" Text="{Binding LogText}" IsReadOnly="True" AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas" FontSize="12" TextWrapping="NoWrap" TextChanged="LogBox_OnTextChanged"/>
    </Grid>
</UserControl>
